<!DOCTYPE html>
<html>
<head>
  <title>Movies</title>
</head>
<body>
  <h1>List of Movies</h1>
  <% if (movies.length > 0) { %>
    <ul>
      <% movies.forEach(function(movie) { %>
        <li><%= movie.title %> - <%= movie.director %> (<%= movie.year %>)</li>
      <% }); %>
    </ul>
  <% } else { %>
    <p>No movies found.</p>
  <% } %>
</body>
</html>